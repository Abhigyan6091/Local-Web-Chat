{
  "name": "group-chat-app",
  "version": "1.0.0",
  "description": "Real-time Group Chat Frontend (WebSocket-ready) — Load Balanced",
  "scripts": {
    "backend1": "python -m uvicorn server.main:app --host 0.0.0.0 --port 8001",
    "backend2": "python -m uvicorn server.main:app --host 0.0.0.0 --port 8002",
    "backend3": "python -m uvicorn server.main:app --host 0.0.0.0 --port 8003",
    "loadbalancer": "python ../Distributed-Load-Balancer/src/load_balancer/balancer.py --host 0.0.0.0 --port 8000 --backends http://127.0.0.1:8001,http://127.0.0.1:8002,http://127.0.0.1:8003 --algorithm round_robin",
    "frontend": "vite --host 0.0.0.0 --port 5000",
    "dev": "concurrently -n \"BACKEND1,BACKEND2,BACKEND3,LB,FRONTEND\" -c \"blue,cyan,green,yellow,magenta\" \"npm run backend1\" \"npm run backend2\" \"npm run backend3\" \"npm run loadbalancer\" \"npm run frontend\"",
    "start": "npm run dev",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "vite": "^5.2.0"
  }
}
