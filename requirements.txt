# Backend nodes (Sys2/Sys3/Sys4)
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
asyncpg>=0.29.0
cryptography>=42.0.0

# Load balancer (Sys1) — uvloop is optional but used when present
uvloop>=0.19.0

# Deployment, load generation, experiments and plots
paramiko>=3.4.0
aiohttp>=3.9.0
matplotlib>=3.8.0
numpy>=1.26.0
pytest>=8.0.0
